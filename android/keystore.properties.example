# Keystore properties for release signing
# Generate keystore: keytool -genkey -v -keystore release.keystore -alias localai-key -keyalg RSA -keysize 2048 -validity 10000
STORE_FILE=release.keystore
STORE_PASSWORD=your_store_password_here
KEY_ALIAS=localai-key
KEY_PASSWORD=your_key_password_here
